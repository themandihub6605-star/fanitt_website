/**
 * Service layer placeholder.
 *
 * This project is UI/UX-only for now — no backend, API, auth, or
 * database calls are wired up. Real API clients (e.g. axios/fetch
 * wrappers for auth, sessions, campaigns, payments) should be added
 * here once the backend contract is ready, keeping components free
 * of direct fetch calls.
 */

export {};
